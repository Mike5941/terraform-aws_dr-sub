#.terraform 디렉토리: Terraform 초기화 중 생성되는 프로바이더 플러그인들이 저장되는 곳

**/.terraform/*

#.tfstate 파일들: 상태 파일들은 민감한 정보를 포함할 수 있으며, 보통 Terraform Cloud나 S3와 같은 원격 백엔드에 저장
*.tfstate
*.tfstate.*

# 크래시 로그 파일들: Terraform이 예기치 않게 종료될 때 생성되는 로그 파일들
crash.log

# 로컬 변수 설정 파일들: 로컬에서만 사용되는 변수 설정 파일들
terraform.tfvars
terraform.tfvars.json

# Override 파일들: 특정 파일들을 재정의(override)하기 위해 사용되는 Terraform 설정 파일들
override.tf
ovrride.tf.json
*_oveerride.tf
*_override.tf.json

#.terraform 디렉토리: Terraform 초기화 중 생성되는 프로바이더 플러그인들이 저장되는 곳입니다.
.terraformrc
terraform.rc

.terraform.lock.hcl

.idea/

*.outputs.tf
.outputs.tf.*

*.zip